import common.first_names as first_names
import common.last_names as last_names
import common.emails as emails
import common.usernames as usernames

first_names = first_names.first_names
last_names = last_names.last_names
emails = emails.emails
usernames = usernames.usernames
